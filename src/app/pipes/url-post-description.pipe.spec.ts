import { UrlPostDescriptionPipe } from './url-post-description.pipe';

describe('UrlPostDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlPostDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
